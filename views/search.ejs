<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Search User</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css">

  <style>
    body {
      background: linear-gradient(135deg, #dbe6f6 0%, #c5796d 100%);
      font-family: 'Poppins', sans-serif;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .search-card {
      background-color: #fff;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
      padding: 30px 40px;
      width: 100%;
      max-width: 450px;
      text-align: center;
      animation: fadeIn 0.6s ease;
    }

    h2 {
      font-weight: 600;
      color: #333;
      margin-bottom: 25px;
    }

    label {
      font-weight: 500;
    }

    .form-control {
      border-radius: 10px;
      padding: 10px 15px;
      font-size: 16px;
      border: 1px solid #ccc;
    }

    .form-control:focus {
      border-color: #198754;
      box-shadow: 0 0 5px rgba(25, 135, 84, 0.4);
    }

    .btn-search {
      background-color: #198754;
      border: none;
      border-radius: 10px;
      padding: 10px 20px;
      font-size: 16px;
      font-weight: 500;
      color: white;
      transition: all 0.3s ease;
    }

    .btn-search:hover {
      background-color: #157347;
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>

<body>
  <div class="search-card">
    <h2><i class="fa-solid fa-magnifying-glass"></i> Search User</h2>
    <form method="POST" action="/user/searched">
      <div class="mb-3 text-start">
        <label for="search" class="form-label">Enter User ID</label>
        <input type="text" name="searchId" class="form-control" id="search" placeholder="e.g. 1023" required>
      </div>
      <button type="submit" class="btn-search">
        <i class="fa-solid fa-search"></i> Search
      </button>
    </form>
  </div>
</body>
</html>
