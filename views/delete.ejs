<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Delete User</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css">

  <style>
    body {
      background: linear-gradient(135deg, #f5dada 0%, #f2b6b6 100%);
      font-family: 'Poppins', sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .delete-card {
      background-color: #fff;
      border-radius: 15px;
      padding: 35px 40px;
      width: 100%;
      max-width: 500px;
      text-align: center;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
      animation: fadeIn 0.6s ease;
    }

    h2 {
      font-weight: 600;
      color: #b02a37;
      margin-bottom: 20px;
    }

    p {
      color: #555;
      margin-bottom: 25px;
    }

    input[type="text"], input[type="password"] {
      width: 100%;
      border-radius: 10px;
      border: 1px solid #ccc;
      padding: 10px 15px;
      font-size: 16px;
      margin-bottom: 15px;
      transition: all 0.3s ease;
    }

    input:focus {
      border-color: #dc3545;
      box-shadow: 0 0 5px rgba(220, 53, 69, 0.4);
      outline: none;
    }

    .btn-delete {
      background-color: #dc3545;
      color: white;
      border: none;
      border-radius: 10px;
      padding: 10px 25px;
      font-size: 16px;
      font-weight: 500;
      transition: all 0.3s ease;
    }

    .btn-delete:hover {
      background-color: #bb2d3b;
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    }

    .btn-back {
      display: inline-block;
      margin-top: 15px;
      color: #6c757d;
      text-decoration: none;
      font-weight: 500;
      transition: color 0.2s ease;
    }

    .btn-back:hover {
      color: #343a40;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>

<body>
  <div class="delete-card">
    <h2><i class="fa-solid fa-triangle-exclamation"></i> Delete User</h2>
    <p>Confirm details to delete user: <strong><%= user.user %></strong></p>

    <form method="POST" action="/user/<%= user.id %>?_method=DELETE">
      <input type="text" name="newEmail" placeholder="Enter email" required>
      <input type="password" name="password" placeholder="Enter password" required>

      <button type="submit" class="btn-delete">
        <i class="fa-solid fa-trash"></i> Delete User
      </button>
    </form>

    <a href="/user" class="btn-back">
      <i class="fa-solid fa-arrow-left"></i> Back to All Users
    </a>
  </div>
</body>
</html>


