<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Username</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css">

  <style>
    body {
      background: linear-gradient(135deg, #f0f2f5 0%, #c9d6ff 100%);
      font-family: 'Poppins', sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .edit-card {
      background-color: #fff;
      border-radius: 15px;
      padding: 35px 40px;
      width: 100%;
      max-width: 500px;
      text-align: center;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
      animation: fadeIn 0.6s ease;
    }

    h2 {
      font-weight: 600;
      color: #333;
      margin-bottom: 25px;
    }

    textarea {
      width: 100%;
      height: 80px;
      resize: none;
      border-radius: 10px;
      border: 1px solid #ccc;
      padding: 10px 15px;
      font-size: 16px;
      transition: all 0.3s ease;
      margin-bottom: 15px;
    }

    textarea:focus {
      border-color: #198754;
      box-shadow: 0 0 5px rgba(25, 135, 84, 0.4);
      outline: none;
    }

    input[type="password"] {
      width: 100%;
      border-radius: 10px;
      border: 1px solid #ccc;
      padding: 10px 15px;
      font-size: 16px;
      margin-bottom: 20px;
      transition: all 0.3s ease;
    }

    input[type="password"]:focus {
      border-color: #198754;
      box-shadow: 0 0 5px rgba(25, 135, 84, 0.4);
      outline: none;
    }

    .btn-edit {
      background-color: #198754;
      color: white;
      border: none;
      border-radius: 10px;
      padding: 10px 25px;
      font-size: 16px;
      font-weight: 500;
      transition: all 0.3s ease;
    }

    .btn-edit:hover {
      background-color: #157347;
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    }

    .btn-back {
      display: inline-block;
      margin-top: 15px;
      color: #198754;
      text-decoration: none;
      font-weight: 500;
      transition: color 0.2s ease;
    }

    .btn-back:hover {
      color: #0d5034;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>

<body>
  <div class="edit-card">
    <h2><i class="fa-solid fa-user-pen"></i> Edit User</h2>
    <p class="text-muted mb-4">You are about to edit the user: <strong><%= user.email %></strong></p>

    <form method="POST" action="/user/<%= user.id %>?_method=PATCH">
      <textarea name="newUsername" placeholder="Enter new username" required ><%= user.user %></textarea>
      <textarea  name="newEmail" placeholder="Enter new email" ><%= user.email %></textarea>

      <input type="password" name="password" placeholder="Enter password" required>
      

      <button type="submit" class="btn-edit"><i class="fa-solid fa-pen-to-square"></i> Save Changes</button>
    </form>

    <a href="/user" class="btn-back"><i class="fa-solid fa-arrow-left"></i> Back to All Users</a>
  </div>
</body>
</html>
