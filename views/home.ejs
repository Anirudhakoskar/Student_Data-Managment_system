<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home Page</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css">

  <style>
    body {
      background: linear-gradient(135deg, #e0eafc 0%, #cfdef3 100%);
      font-family: 'Poppins', sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
    }

    .home-card {
      background: #fff;
      border-radius: 20px;
      padding: 50px 40px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
      animation: fadeIn 0.6s ease;
    }

    h1 {
      color: #212529;
      font-weight: 600;
      margin-bottom: 20px;
    }

    h2 {
      color: #333;
      margin-bottom: 30px;
      font-weight: 500;
    }

    .btn-main {
      background-color: #198754;
      color: white;
      border: none;
      border-radius: 10px;
      padding: 12px 25px;
      font-size: 17px;
      font-weight: 500;
      margin: 10px;
      transition: all 0.3s ease;
    }

    .btn-main:hover {
      background-color: #157347;
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    }

    .btn-view {
      background-color: #0d6efd;
    }

    .btn-view:hover {
      background-color: #0b5ed7;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>

<body>
  <div class="home-card">
    <h1><i class="fa-solid fa-house-user"></i> Welcome to Student Management System</h1>
    <h2>The total number of students are: <strong><%= count %></strong></h2>

   <form method="GET" action="/user/add">
      <button class="btn btn-success buttons">
        ADD USER <i class="fa-solid fa-user-plus"></i>
      </button>
    </form>

    <a href="http://localhost:8080/user">
      <button class="btn-main btn-view">
        <i class="fa-solid fa-users"></i> View All Users
      </button>
    </a>
  </div>
</body>
</html>
